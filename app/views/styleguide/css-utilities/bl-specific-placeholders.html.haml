= render 'styleguide/partials/shared'

.card.styleguide-intro.copy--body.context--content
  .clearfix
    %h1.copy--h1 Bellroy Specific Placeholders
    %p
      These are helper placeholders which provide often used styling specific to BL.

- @app.get_css("sass/extends/bl_styles/_decoration").each do |css|
  = render "styleguide/partials/snippet", properties: css

- @app.get_css("sass/extends/bl_styles/_transitions").each do |css|
  = render "styleguide/partials/snippet", properties: css
