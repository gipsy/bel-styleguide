:javascript
  (function(){
    if (window.bl.enhanced && 
        window.bl.supports.localStorage && 
        window.localStorage.getItem('_blfont') != "true") {

      window.onload = function() {
        var xhr = new XMLHttpRequest(),
            fontPath = window.bl.supports.woff2 ? "#{font_path('fonts_woff2.css')}" : "#{font_path('fonts.css')}";

        xhr.open('GET', fontPath);

        xhr.onload = function (e) {
          window.localStorage.setItem('_blfont', 'true')
        };

        xhr.send();
      }

    }
  })()
